#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
typedef long long ll;

int main()
{
    // 入力
    int N;
    cin >> N;

    vector<int> A(N), B(N);
    rep (i, N) {
        cin >> A[i];
    }
    rep (i, N) {
        cin >> B[i];
    }

    // 出力
    cout << a + b << endl;
    return 0;
}

